# Configuração do Prisma (copiar do .env na raiz)
# DATABASE_URL=postgresql://user:password@localhost:5432/tuya_locks_db?schema=public

# Instruções:
# 1. Configure DATABASE_URL no arquivo .env raiz
# 2. Execute migrations: npx prisma migrate dev
# 3. Gere client: npx prisma generate
# 4. Popule dados: npm run db:seed
# 5. Visualize (opcional): npm run db:studio
